# sample_docs

Place your source documents (`.pdf`, `.docx`) in this folder.

This file is intentionally kept so the `sample_docs/` directory is tracked in git.

Quick start:

```bash
rag ingest sample_docs
rag ask "Your question here"
```
